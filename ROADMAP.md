# ccswitch 로드맵

## 최종 결과물

- 대상 사용자: 터미널에서 Claude Code CLI를 쓰며 계정이 둘 이상인 개발자
- 핵심 사용자 가치: 로그아웃·재로그인 없이 명령 하나로 계정을 바꿔 Claude Code를 실행한다.
  계정별 자격증명과 세션 기록은 분리되지만, 손에 익은 설정(`CLAUDE.md`, 스킬, 커맨드, 에이전트)은
  그대로 따라온다.
- 목표 제공 수준: 공개 저장소의 오픈소스 CLI. 소스 공개와 `go install` 안내까지가 범위이며,
  릴리스 바이너리나 패키지 매니저 배포는 하지 않는다. Windows와 macOS에서 동작한다.
- 완성 상태: Windows와 macOS 사용자가 저장소 README만 보고 설치해, 여러 계정을 프로필로 등록하고
  프로필을 지정해 Claude Code를 실행할 수 있다. 자격증명을 직접 만지지 않고 Claude Code의
  기존 인증 경로를 그대로 쓴다.

## 서비스 완료 기준

1. 서로 다른 두 계정으로 로그인한 프로필을 각각 지정해 Claude Code를 실행했을 때,
   각 세션의 `claude auth status`가 서로 다른 계정을 보고한다.
2. 새로 만든 프로필에서 default 프로필의 `CLAUDE.md`, `commands/`, `skills/`, `agents/`, `rules/`가
   그대로 적용된다.
3. 프로필을 삭제해도 공유 원본(`~/.claude` 쪽 실체)이 남아 있고, 같은 이름으로 다시 만들었을 때
   이전 계정의 자격증명으로 인증되지 않는다.
4. 위 1~3이 Windows와 macOS 양쪽에서 동일하게 관찰된다.
5. 처음 보는 사용자가 README에 적힌 설치·사용 절차만 따라 1~3에 도달한다.

## 포함 범위

- 프로필 메타데이터 관리: 프로필 생성·조회·삭제, 이름 규칙, 메타데이터 파일의 플랫폼별 위치
- 프로필 단위 실행: `CLAUDE_CONFIG_DIR` 주입과 `claude` 실행, 인자 전달,
  인증 관련 환경변수 제거를 통한 격리 보장
- 프로필 단위 인증: 세션을 띄우지 않고 해당 프로필로 로그인·로그아웃
- 설정 자산 공유: 디렉토리는 링크(Windows junction / Unix symlink), `CLAUDE.md`는 import 참조,
  `settings.json`은 복사와 명시적 재동기
- 기존 `~/.claude` 환경을 이름 있는 프로필로 승격
- 프로필 목록에 계정 정보 표시

계획하고 있는 명령 표면은 다음과 같다. 각 명령의 정확한 동작과 완료 조건은 해당 feature 문서에서
확정한다.

| 명령 | 하는 일 |
|---|---|
| `add <name>` | 프로필 디렉토리 생성과 공유 자산 구성 |
| `login <name>` | 해당 프로필로 로그인 |
| `logout <name>` | 해당 프로필로 로그아웃 |
| `use [name] [-- args]` | 프로필을 지정해 Claude Code 실행 (생략 시 default) |
| `list` | 프로필과 계정 정보 조회 |
| `rm <name>` | 프로필 삭제 |
| `import` | 현재 `~/.claude`를 이름 있는 프로필로 승격 |
| `sync` | 공유 파일 재동기 |

## 마일스톤

### M1. Windows에서 프로필을 나눠 실행할 수 있다

프로필을 만들고, 목록을 보고, 지정해서 실행하고, 지우는 최소 흐름이 동작한다. 로그인은 아직
Claude Code 세션 안에서 직접 한다.

- 의존 관계: 없음
- 전환 기준: 서로 다른 두 계정으로 로그인한 프로필을 번갈아 실행했을 때 각 세션이 서로 다른
  계정으로 인증되고, 한쪽 세션 기록이 다른 쪽에 나타나지 않는다
- Feature 문서 후보: `profile-store`, `profile-launcher`

### M2. 인증을 프로필 단위로 다룰 수 있다

세션을 띄우지 않고 프로필별로 로그인·로그아웃한다. 삭제할 때 자격증명 정리를 Claude Code에
위임해, 설정 디렉토리 밖에 자격증명을 두는 플랫폼에서도 잔여물이 남지 않게 한다.

- 의존 관계: M1
- 전환 기준: 대화형 세션을 열지 않고 프로필 로그인·로그아웃이 완료되며, 삭제 후 같은 이름으로
  다시 만들었을 때 이전 계정으로 인증되지 않는다
- Feature 문서 후보: `profile-auth`

### M3. 설정 자산을 프로필 간 공유한다

새 프로필이 빈 상태에서 시작하지 않고 default의 작업 환경을 그대로 물려받는다. 기존 `~/.claude`를
프로필로 승격하는 경로도 여기서 만든다.

- 의존 관계: M1
- 전환 기준: 새로 만든 프로필에서 default의 스킬·커맨드·에이전트·메모리가 동작하고,
  그 프로필을 삭제해도 원본이 그대로 남는다
- Feature 문서 후보: `shared-assets`, `profile-import`

### M4. macOS에서 동일하게 동작한다

플랫폼 분기를 채우고 macOS에서 격리가 실제로 성립하는지 확인한다. macOS는 자격증명을 설정
디렉토리가 아닌 Keychain에 두므로, 프로필별로 자격증명이 갈리는지가 이 단계의 전제다.

- 의존 관계: M1, M2, M3
- 전환 기준: macOS 실기에서 서비스 완료 기준 1~3이 관찰되고, 프로필 레이아웃이 Windows와
  동일하다
- Feature 문서 후보: `macos-support`

## 최종 관문

여러 마일스톤에 공통으로 적용한다.

- 플랫폼별로 갈리는 동작(실행 방식, 링크 생성, 경로 규칙)은 순수 로직과 분리해, 플랫폼에
  의존하지 않는 부분은 어느 환경에서도 테스트할 수 있어야 한다
- macOS 동작은 로컬에서 검증할 수 없으므로 CI에서 확인한다. M4 시점에 Windows와 macOS
  양쪽에서 테스트가 통과해야 한다
- 삭제 계열 동작은 링크를 따라 들어가 공유 원본을 지우지 않는다는 것이 테스트로 확인되어야 한다
- README의 설치·사용 절차가 실제 동작과 일치한다
- MIT `LICENSE` 파일이 저장소에 있다

## 보류·제외 범위

### Claude 데스크톱 앱과 IDE 확장 지원

앱은 시작 메뉴에서 실행되어 ccswitch를 거치지 않고, 자기 번들 Claude Code를 자식 프로세스로
띄운다. 게다가 앱 셸의 로그인은 별도 웹 세션이라 설정 디렉토리를 바꿔도 함께 바뀌지 않는다.
설정 디렉토리만 전환하면 앱 UI와 그 안의 Claude Code가 서로 다른 계정을 쓰는 상태가 될 수 있다.
도구가 반쯤 전환된 상태를 만드는 것은 계정을 잘못 쓰는 사고로 이어진다.

다시 검토할 조건: 앱이 설정 디렉토리 전환을 공식 지원하거나, 앱 계정과 내부 Claude Code 계정을
함께 바꾸는 경로가 생겼을 때.

### 기본 프로필 영구 고정

레지스트리나 셸 설정에 `CLAUDE_CONFIG_DIR`을 영구 기록해 기본 계정을 바꾸는 기능. 사용자가
셸 프로필에 한 줄을 추가하면 같은 결과를 얻을 수 있어 도구가 소유할 이유가 약하다. 반면 도구가
쓰면 눈에 보이지 않는 전역 상태가 남아, 나중에 어느 계정으로 실행 중인지 추적하기 어려워진다.

다시 검토할 조건: 매번 프로필을 지정하는 것이 실사용에서 부담으로 확인되었을 때.

### 자격증명 직접 조작

자격증명 파일이나 Keychain 항목을 직접 교체해 계정을 바꾸는 방식. 설정과 기록을 완전히 공유할 수
있다는 장점이 있지만, Claude Code가 실행 중에 계속 쓰는 파일을 밖에서 덮어쓰게 되어 토큰 갱신과
경합하면 로그인 상태가 깨진다. 플랫폼마다 저장소가 달라 구현도 갈라진다.

다시 검토할 조건: 설정 자산 공유(M3)로도 해결되지 않는 요구가 남았을 때.

### Linux 명시 지원

macOS와 같은 Unix 분기를 쓰므로 실질적으로는 동작할 가능성이 높지만, 자격증명 저장소가 달라
검증이 별도로 필요하다. 검증하지 않은 것을 지원한다고 적지 않는다.

다시 검토할 조건: 사용 요청이 있고 검증 환경을 확보했을 때.

### 릴리스 바이너리와 패키지 매니저 배포

목표 제공 수준을 소스 공개로 정했다. 배포 채널을 늘리면 버전 관리와 서명 등 유지 부담이 함께
늘어난다.

다시 검토할 조건: 소스 설치가 어려운 사용자층이 실제로 생겼을 때.
