# profile-auth — IMPLEMENT

- [x] task-001: `login <name>`이 대화형 세션 없이 그 프로필의 로그인 절차를 시작한다
  - 목적: `ccswitch login work`가 work 프로필 설정 디렉토리를 보는 Claude Code 인증 절차를
    같은 창에서 시작하고, `--` 뒤에 준 값을 해석하지 않고 그 절차에 넘기며, 절차가 끝나면
    Claude Code의 종료 코드가 그대로 ccswitch의 종료 코드가 된다. 등록되지 않은 이름,
    디렉토리를 쓸 수 없는 프로필, PATH에서 실행 파일을 찾지 못한 경우에는 `use`와 같은
    내용·같은 종료 코드로 거부되고 프로세스가 한 번도 뜨지 않는다.
  - 접근: `claude auth` 하위 명령 인자를 만드는 순수 경계를 새로 세우고, 그 경계가 만든
    `auth login` 인자 뒤에 사용자가 `--` 뒤에 준 값을 붙여 기존 콘솔 인계 실행에 넘긴다.
    `use`가 쓰던 대상 해석(이름 → `default` 분기 → 형식 검증 → 등록 확인 → 디렉토리 상태),
    실행 파일 이름, `--` 전후 분리를 네 커맨드가 함께 쓰도록 공유하고 `use` 전용으로 적힌
    주석을 함께 고친다. 자식 종료 코드를 나르는 error에 메시지가 붙지 않도록 `use`가 쓰는
    출력 경로를 같이 쓰고, `--` 없이 온 알 수 없는 옵션에는 같은 안내 한 줄을 덧붙인다.
    위치 인자는 정확히 하나로 받고 생략을 허용하지 않으며, 성공 경로에서 ccswitch 자신은
    한 줄도 내지 않는다.
  - 검증 조건:
    - 결과: 등록된 이름으로 실행하면 조회로 확정된 실행 파일 경로에 `auth login` 뒤로 `--`
      뒤 값이 순서·개수 그대로 붙은 인자가 넘어가고, 자식 환경에 `CLAUDE_CONFIG_DIR`이
      `list`가 보여주는 것과 같은 경로 문자열로 한 번만 들어 있으며
      `CLAUDE_SECURESTORAGE_CONFIG_DIR`은 들어 있지 않다. `default`를 이름으로 주면 두 변수
      모두 들어 있지 않다. 자식 종료 코드가 0이면 0, 0이 아니면 같은 값이 그대로 나가며 그
      경우 ccswitch가 stdout·stderr에 아무것도 덧붙이지 않는다. 위치 인자가 0개거나 2개
      이상이면 코드 2다. 미등록 이름은 3, `missing`·`unusable`은 6, 조회 실패는 7이고 각
      메시지가 같은 상황의 `use` 메시지와 같은 문자열이다. 거부 경로에서는 프로세스가 뜨지
      않고 등록 파일과 `list` 출력이 실행 전후로 같다.
    - 확인: 기록용 실행 대역으로 `add`한 프로필에 `login`을 걸어 기록된 경로·인자·환경을
      확인하는 테스트, 자식 종료 코드 0·1·2·130을 중계하는 table-driven 테스트, 거부 네
      경우를 도는 테스트를 둔다. 거부 테스트는 같은 이름·같은 상태로 `use`와 `login`을 각각
      돌려 stderr 한 줄이 접두사까지 같은 문자열인지 비교하고, 조회 실패 케이스는
      `exec.ErrNotFound`를 감싼 오류를 돌려주는 대역으로 만든다. `add`·`list`·`rm`·`use`의
      기존 테스트가 수정 없이 통과하고 `go build ./...`, `go vet ./...`,
      `go test -count=1 ./...`가 통과한다. 실제 브라우저 절차를 밟은 뒤 그 프로필의 인증
      상태가 로그인된 것으로 나오는 것은 이 테스트들이 덮지 못한다 — 브라우저 OAuth가 필요한
      부분의 관찰은 task-006이 소유한다.
  - 참조: SPEC §5.1, §5.3, §5.4, §5.11 / ANALYSIS §1, §2, §3, §5 D1, D3, D5

- [x] task-002: `list --accounts`가 프로필별 계정과 플랜을 보여주고 옵션 없는 출력은 그대로다
  - 목적: `ccswitch list --accounts`가 기존 세 열 뒤에 `ACCOUNT`·`PLAN` 열을 더해 로그인된
    프로필의 계정 식별 값과 플랜을 보여주고, 로그인되지 않은 프로필·조회가 실패한 프로필·
    조회하지 않은 프로필을 서로 구별되는 표기로 보여준다. 조회가 실패한 행은 stderr에 무엇이
    실패했는지와 조치가 남고 전체 종료 코드가 0이 아니지만 표는 그대로 나온다. 옵션 없는
    `ccswitch list`의 출력은 이 변경 전후로 같고 계정 조회를 위한 프로세스가 하나도 뜨지
    않는다.
  - 접근: 자식을 띄워 표준 출력·표준 오류·종료 코드를 값으로 받아 오는 실행을 기존 실행
    인터페이스에 더한다 — 부모 콘솔을 넘기지 않고, 자식 표준 입력은 비우고, 인터럽트 무시를
    걸지 않는다. 인증 경계에 `claude auth status --json` 인자 생성과 결과 판정을 더해, 표준
    출력이 `loggedIn`을 담은 JSON 객체로 읽히면 종료 코드와 무관하게 답으로 보고 그렇지
    않으면 캡처한 출력과 종료 코드를 담은 실패로 본다. 옵션이 있을 때만 PATH 조회를 한 번
    하고, 디렉토리 상태가 `ok`인 행만 행 순서대로 하나씩 조회한다. 인터페이스에 메서드가
    늘어 깨지는 기존 실행 대역 둘은 각 대역의 목적에 맞게 채운다.
  - 검증 조건:
    - 결과: 옵션을 주면 헤더가 `NAME`·`DIR`·`STATUS`·`ACCOUNT`·`PLAN`이고, 로그인된 행은
      `email`(비어 있으면 `authMethod`)과 `subscriptionType`을, 로그인되지 않은 행은
      `logged-out`과 `-`를, 조회가 실패한 행은 `unknown`과 `-`를, 상태가 `ok`가 아니어서
      조회하지 않은 행은 `-`와 `-`를 담는다. 어느 열 값에도 공백이 들어가지 않는다. 조회
      횟수는 `ok` 행 수와 같고 PATH 조회는 한 번이다. 조회 실패가 하나라도 있으면 종료 코드
      8이고 실패한 프로필과 조치를 담은 줄이 stderr에 남으며 표는 stdout에 그대로 나온다.
      로그인되지 않음과 조회하지 않음은 종료 코드를 바꾸지 않는다. PATH 조회가 실패하면 표를
      내지 않고 코드 7이다. 옵션이 없으면 헤더와 행이 변경 전과 같고 조회·실행이 한 번도
      일어나지 않으며, 상태가 `ok`가 아닌 프로필의 디렉토리가 조회 때문에 새로 만들어지지
      않는다.
    - 확인: 종료 코드와 표준 출력·표준 오류를 값으로 지정하는 캡처 대역으로 로그인·로그인
      안 됨·조회 실패·상태 불량 네 행을 한 표에 섞어 돌리는 테스트를 두고, 판정 계층은
      프로세스를 띄우지 않는 table-driven 테스트로 덮는다 — 종료 코드 1인
      `{"loggedIn": false, ...}`가 답으로 읽히고, `loggedIn`이 없는 JSON과 JSON이 아닌 출력이
      실패로 읽히는 케이스를 포함한다. 더해서 실제 프로세스 왕복을 한 번 검증한다: 테스트
      바이너리 자신을 조회 결과 실행 파일로 지정해 자식이 표준 출력에 `loggedIn`을 담은
      JSON을 내고 0이 아닌 코드로 끝나게 하고, 부모가 그것을 답으로 읽어 그 행을 계정으로
      표시하는지 본다. 옵션 없는 `list`는 실행 경계를 채우지 않은(nil) 기존 테스트가 수정
      없이 통과하는 것으로 프로세스 미실행을 확인하고, 기록용 대역을 채운 경우에도 조회·실행
      기록이 0인지 함께 본다 — 기존 명령의 출력과 종료 코드 계약을 깨지 않는다는 spec.md §3
      제약이 여기 걸린다. `add`·`rm`·`use`의 기존 테스트와 알 수 없는 플래그가 코드 2가 되는
      기존 테스트가 수정 없이 통과하고 `go build ./...`, `go vet ./...`,
      `go test -count=1 ./...`가 통과한다.
  - 참조: SPEC §5.5, §5.6, §5.11 / ANALYSIS §1, §2, §3, §5 D2, D4, D6, D7, D8, D13

- [x] task-003: `logout <name>`이 로그아웃을 마치고 결과를 프로필 이름과 함께 알린다
  - 목적: `ccswitch logout work`가 work 프로필 설정 디렉토리에 대해 Claude Code 로그아웃을
    실행하고, 성공하면 어느 프로필이 로그아웃됐는지 담은 한 줄을 stderr에 남기고 종료 코드
    0으로 끝난다. 위임한 명령이 답을 주지 못하면 그 이유와 조치가 stderr에 나오고 종료 코드
    8이 된다. 거부 조건과 그때의 종료 코드는 `use`·`login`과 같다.
  - 접근: 인증 경계에 `auth logout` 인자 생성과 결과 판정(0이면 성공, 그 밖은 캡처한 출력을
    이유로 담은 실패)을 더하고 task-002가 세운 캡처 실행으로 부른다. 대상 해석은 `login`과
    같은 것을 쓰고, 위치 인자는 정확히 하나로 받으며 `--` 뒤 전달은 받지 않는다. 성공 결과
    한 줄은 `add`·`rm`이 쓰는 자리와 형태에 맞춘다. 새 종료 코드 하나를 코드 표에 더하고
    기존 0~7 매핑과 자식 종료 코드 우선 규칙은 그대로 둔다.
  - 검증 조건:
    - 결과: 캡처 실행에 넘어간 인자가 `auth logout`이고 그 뒤에 아무것도 붙지 않으며, 자식
      환경에 그 프로필 디렉토리가 `CLAUDE_CONFIG_DIR`로 한 번만 들어 있고
      `CLAUDE_SECURESTORAGE_CONFIG_DIR`은 없다. `default`를 이름으로 주면 두 변수 모두 없다.
      성공하면 stdout이 비어 있고 stderr 한 줄에 프로필 이름이 들어가며 코드 0이다. 캡처
      결과 종료 코드가 0이 아니거나 자식을 띄우지 못하면 코드 8이고, stderr에 캡처한 출력이
      나른 이유와 사용자가 취할 조치가 들어간다. `--` 뒤에 무엇을 주면 코드 2이고 프로세스가
      뜨지 않는다. 미등록 이름 3, `missing`·`unusable` 6, 조회 실패 7이며 각 메시지가 같은
      상황의 `use` 메시지와 같은 문자열이고, 그 경로에서는 등록 파일과 `list` 출력이 실행
      전후로 같다. 기존 0~7 매핑은 코드 표에 8이 더해진 뒤에도 그대로다.
    - 확인: 캡처 대역이 코드 0을 돌려주는 성공 케이스와 0이 아닌 코드·오류를 돌려주는 실패
      두 케이스로 종료 코드와 stderr 내용을 확인하는 테스트, 넘어간 인자·환경을 확인하는
      테스트, `use`·`logout`의 거부 메시지를 세 상황에서 문자열로 비교하는 테스트를 둔다.
      코드 표에 분기를 더한 뒤에도 기존 0~7 매핑 테스트와 `add`·`list`·`rm`·`use`·`login`
      테스트가 수정 없이 통과하고 `go test -count=1 ./...`가 통과한다. 실제 자격증명이 있는
      프로필을 로그아웃한 뒤 그 프로필의 인증 상태가 로그인되지 않은 것으로 나오는 것은 이
      테스트들이 덮지 못한다 — 그 관찰은 task-006이 소유한다.
  - 참조: SPEC §5.2, §5.3, §5.4, §5.11 / ANALYSIS §2, §3, §5 D3, D5, D12

- [x] task-004: `rm`이 삭제 전에 자격증명 정리를 위임하고, 실패하면 아무것도 지우지 않는다
  - 목적: `ccswitch rm work`가 승인을 받은 뒤 work 프로필 설정 디렉토리에 대해 Claude Code
    로그아웃을 먼저 실행하고, 그것이 성공했을 때만 디렉토리와 등록을 지운다. 정리가 실패하면
    디렉토리도 등록도 그대로 남고 stderr에 이유와 조치가 나오며 종료 코드가 8이다. 실행
    파일을 찾지 못하면 승인을 묻기 전에 멈추고, 로그인되지 않은 프로필도 사전 판정 없이
    삭제까지 끝난다.
  - 접근: 기존 순서(이름 판정 → 등록 확인 → 프롬프트 → 디렉토리 삭제 → 등록 삭제 → 저장)
    사이에 두 자리를 넣는다 — 정리를 건너뛰지 않는 경우의 PATH 조회를 프롬프트 앞에, 위임
    실행을 승인 뒤 디렉토리 삭제 앞에 둔다. 위임은 task-003의 인자 생성과 판정을 그대로 쓰고,
    로그인 여부를 미리 묻는 조회는 두지 않는다. 디렉토리 상태 판정도 위임 앞에 두지 않는다.
  - 검증 조건:
    - 결과: 승인 뒤 캡처 실행이 정확히 한 번 일어나고, 그 인자가 `auth logout`이며 자식
      환경의 `CLAUDE_CONFIG_DIR`이 지워질 프로필 디렉토리 하나다. 위임이 성공하면 디렉토리와
      등록이 사라지고 코드 0이며 기존 결과 한 줄이 그대로 나온다. 위임이 0이 아닌 코드로
      끝나거나 자식을 띄우지 못하면 코드 8이고 디렉토리·등록 파일·`list` 출력이 실행 전과
      같으며 stderr에 캡처한 이유와 조치가 들어간다. 프롬프트를 거절하면 위임이 일어나지
      않고 아무것도 지워지지 않으며 코드 0이다. 비대화형에서 `--yes`가 없으면 위임 전에
      코드 2로 끝난다. PATH에서 실행 파일을 찾지 못하면 코드 7이고 프롬프트 문구가 화면에
      나오지 않으며 아무것도 지워지지 않는다. 등록만 있고 디렉토리가 없는 프로필에도 위임이
      일어나고 삭제가 끝난다. 삭제 한 번에 뜨는 프로세스는 하나뿐이다 — 로그인 여부를 묻는
      조회가 없다.
    - 확인: 캡처 대역으로 (1) 위임 성공 → 삭제 완료, (2) 위임이 0이 아닌 코드 → 코드 8이고
      디렉토리·등록 스냅샷이 실행 전과 같음, (3) 위임이 오류로 실패 → 같은 결과, (4) 대화형
      거절 → 위임 기록 없음, (5) 비대화형 `--yes` 없음 → 위임 기록 없고 코드 2, (6) 조회
      실패 대역 → 코드 7이고 프롬프트 문구가 stderr에 없음, (7) 등록만 있고 디렉토리가 없는
      프로필 → 위임 한 번 뒤 삭제 완료를 각각 확인한다. 로그인되지 않은 프로필이 막히지 않는
      것은 대역이 코드 0을 돌려주는 (1)·(7)로 덮고, 그 두 케이스에서 캡처 호출이 한 번뿐임을
      함께 본다. 실행 경계를 채우지 않던 기존 `rm` 테스트 중 삭제까지 가는 것들은 캡처 대역을
      채우도록 고치되 기존 결과·종료 코드 기대값은 바꾸지 않으며, `rm`의 거부 테스트(예약
      이름, 미등록 이름, 형식 위반)는 수정 없이 통과한다. `go test -count=1 ./...`가 통과한다.
  - 참조: SPEC §5.7, §5.8, §5.10 / ANALYSIS §2, §5 D9, D10, D12

- [x] task-005: `rm --skip-logout`이 정리를 건너뛰고 자격증명이 남을 수 있음을 알린다
  - 목적: `ccswitch rm work --skip-logout`이 Claude Code 로그아웃을 위임하지 않고 디렉토리와
    등록을 지우며, 승인을 묻기 전에 자격증명이 남을 수 있다는 경고를 stderr에 낸다. 이
    플래그를 쓰면 PATH 조회도 일어나지 않는다.
  - 접근: 기존 `--yes`와 같은 방식으로 불리언 플래그 하나를 더하고 짧은 형태는 두지 않는다.
    플래그가 켜져 있으면 조회와 위임을 모두 건너뛰고, 경고는 정리를 건너뛴다는 결정이
    확정되는 자리 — 프롬프트보다 앞 — 에서 낸다.
  - 검증 조건:
    - 결과: 플래그를 주면 조회·캡처 기록이 0이고 디렉토리와 등록이 사라지며 코드 0이다.
      stderr에 자격증명이 남을 수 있다는 경고가 프롬프트 문구보다 먼저 나오고, `-y`와 함께
      주어도 그 경고는 나온다. PATH에 실행 파일이 없어도 삭제가 끝난다. 플래그를 주지 않으면
      task-004의 동작이 그대로다.
    - 확인: 조회가 항상 실패하는 대역으로 `rm work --skip-logout --yes`가 코드 0으로 삭제를
      끝내고 조회·캡처 기록이 비어 있음을 확인하는 테스트, 대화형에서 stderr의 경고 줄이
      프롬프트 줄보다 앞에 오는지 확인하는 테스트, 그 상태에서 거절하면 아무것도 지워지지
      않는지 확인하는 테스트를 둔다. 짧은 형태를 두지 않았다는 것은 한 글자 형태를 준 호출이
      코드 2로 거부되는 것으로 확인한다. task-004의 테스트가 수정 없이 통과하고
      `go test -count=1 ./...`가 통과한다.
  - 참조: SPEC §5.9 / ANALYSIS §2, §5 D11

- [ ] task-006: 사용자 실기 관찰로 실계정 로그인·로그아웃과 프로필 격리를 확인한다
  - 목적: 실제 브라우저 절차와 실제 계정으로 `login`·`logout`을 걸었을 때 그 프로필의 인증
    상태가 각각 로그인·로그아웃으로 바뀌고, 이름 있는 프로필에 그 두 명령을 걸어도 기본 설정
    디렉토리와 다른 프로필의 인증 상태는 바뀌지 않는다.
  - 접근: 코드 변경 없이 빌드한 실행 파일로 사용자가 직접 관찰한다. 어긋나는 관찰이 나오면
    그 동작을 소유한 앞선 Task로 되돌린다.
  - 검증 조건:
    - 결과: `ccswitch login <name>`이 대화형 코딩 세션을 띄우지 않고 인증 절차만 밟고
      끝나며, 절차를 마친 뒤 `ccswitch list --accounts`의 그 행이 방금 인증한 계정과 플랜을
      보여준다. `ccswitch logout <name>` 뒤 같은 행이 `logged-out`으로 바뀐다. 두 명령 전후로
      `default` 행과 다른 프로필 행의 계정 값이 그대로다.
    - 확인: 서로 다른 두 계정이 필요하다. 사용자가 프로필 두 개를 만들고 (1) 한쪽에
      `ccswitch login`을 걸어 브라우저 절차를 끝낸 뒤 `ccswitch list --accounts`로 그 행만
      계정이 채워지고 `default` 행과 다른 프로필 행이 그대로인지, (2) 다른 쪽에도 다른
      계정으로 같은 절차를 밟아 두 행이 서로 다른 계정을 보여주는지, (3) 한쪽에
      `ccswitch logout`을 걸어 그 행만 `logged-out`이 되고 나머지 두 행이 그대로인지를
      확인한다. 이 세 관찰은 자동 테스트로 대신할 수 없다 — 브라우저 OAuth와 실제 자격증명이
      필요하다. macOS Keychain에서 정리가 잔여물을 실제로 없애는지는 이번 범위 밖이다.
  - 참조: SPEC §5.1, §5.2, §5.3 / ANALYSIS §5 D13

- [ ] task-007: README의 상태 문구와 사용 절차가 실제 동작과 일치한다
  - 목적: README를 읽은 사용자가 지금 되는 것만 알고 그대로 따라 할 수 있다 — `login`·`logout`
    으로 계정을 붙이고 뗄 수 있다는 것과 `list`가 계정을 보여줄 수 있다는 것, 그 사용법이 적혀
    있고, 로그인을 ccswitch가 다루지 않는다고 읽히는 자리가 남지 않는다.
  - 접근: 상단 상태 문구를 `login`·`logout`과 계정 조회까지 동작하는 것으로 고치고, 로그인을
    실행된 세션 안에서 한다고 적은 단락을 명령으로 한다는 절차로 다시 쓴다. 사용 절차에
    `login`·`logout`·`list --accounts`와 `rm`의 정리 위임·건너뛰기를 넣고, `list --accounts`
    출력 예시는 task-006에서 실제로 본 열과 값으로 적는다. 브라우저에 다른 계정이 붙어 있을
    때의 주의와 프로필당 최초 1회라는 설명은 그대로 두고, 동작 방식 설명에는 같은 설정
    디렉토리 주입을 `claude auth` 하위 명령에도 쓴다는 것을 더한다. 설치 안내는 넣지 않는다.
  - 검증 조건:
    - 결과: 상태 문구에 계정을 붙이고 뗄 수 있다는 내용과 `list`가 계정을 보여줄 수 있다는
      내용이 있고, "로그인은 아직 그 세션 안에서 직접 하고, `list`는 이름·경로·상태만 보여
      준다"와 "로그인은 ccswitch가 다루지 않는다"가 남지 않는다. 사용 절차에 `login`·`logout`·
      `list --accounts`·`rm --skip-logout`이 실제 표면과 같은 형태로 적혀 있고, 아직 없는 명령
      이 쓸 수 있는 것처럼 적힌 자리가 없다. 브라우저 계정 주의 항목과 프로필당 최초 1회 설명
      은 그대로 남아 있다. 설치 절차는 새로 생기지 않는다.
    - 확인: README에 적힌 명령을 위에서부터 순서대로 실제로 실행해 각 출력이 문서에 적힌 모양과
      같은지 사용자가 확인한다. 문서에 남은 명령 이름과 플래그를 `ccswitch --help`·
      `ccswitch rm --help`·`ccswitch list --help` 출력과 맞춰 커맨드 트리에 없는 것이 남지
      않았음을 확인한다.
  - 참조: SPEC §5.12 / ANALYSIS §4, §5 D14
